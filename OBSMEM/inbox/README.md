# Inbox

Сюда складываются новые идеи, ссылки, быстрые заметки и необработанные мысли.

Правило:
- всё отсюда либо уходит в `raw/`, либо превращается в страницу в `sources/`, `analyses/`, `projects/` или `decisions/`.
